/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['i.pinimg.com',"img.clerk.com", "assets.pinterest.com", "img.icons8.com"],
      },
};

export default nextConfig;
